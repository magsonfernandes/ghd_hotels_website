import type { CSSProperties } from "react";

/** Hero H1 on image heroes — same scale as Nivaãra (Instrument Serif). */
export const heroImageTitleStyle: CSSProperties = {
  fontWeight: 400,
  WebkitTextStroke: "0.6px rgba(0, 0, 0, 0.75)",
  textShadow:
    "0 0 20px rgba(0,0,0,0.75), 0 0 40px rgba(0,0,0,0.6), 0 0 70px rgba(0,0,0,0.85)",
};
